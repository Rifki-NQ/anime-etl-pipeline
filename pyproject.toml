[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "anime-etl-pipeline"
version = "0.0.1"
requires-python = ">=3.10"

dependencies = [
    "httpx>=0.28.1",
    "pydantic>=2.13.4"
]

[project.optional-dependencies]
dev = [
    "pytest>=9.0.3",
    "pytest_asyncio>=1.3.0",
    "mypy>=2.1.0",
]

[project.scripts]
anime-etl-pipeline = "src.main:main"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.pytest.ini_option]
asyncio_mode = "auto"

[tool.mypy]
strict = true